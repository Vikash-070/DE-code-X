import { AIProvidersPage } from "@/features/settings/ai-providers-page";

export const metadata = {
  title: "AI Providers · DE-code X",
  description: "Configure your AI provider API keys for orchestration."
};

export default function SettingsPage() {
  return <AIProvidersPage />;
}
