import { SessionsPage } from "@/features/workspace/sessions-page";

export const metadata = {
  title: "Sessions — DE-code X"
};

export default function SessionsRoute() {
  return <SessionsPage />;
}
